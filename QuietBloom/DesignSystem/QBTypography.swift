import SwiftUI

enum QBTypography {
    static let eyebrow: Font = .caption.weight(.semibold)
    static let title: Font = .title2.weight(.semibold)
    static let sectionTitle: Font = .headline
    static let body: Font = .body
    static let quiet: Font = .subheadline
}
